"""
Common app models
This file exists to prevent Django test framework issues with apps that don't have models
"""
from django.db import models

# This app intentionally left blank - no database models needed